# Optional OAuth scope (provider-specific format)
CLAWAUTH_SCOPE=""

# Optional CLI tuning
CLAWAUTH_POLL_INTERVAL_MS="2000"
CLAWAUTH_POLL_TIMEOUT_MS="300000"

# Keychain storage
CLAWAUTH_KEYCHAIN_SERVICE="clawauth"
CLAWAUTH_KEYCHAIN_ACCOUNT="github:default"

# Never store these in .env for OSS repos:
# - <PROVIDER>_CLIENT_SECRET (Worker secret)
# - STATE_SIGNING_SECRET (Worker secret)
# Keep all runtime secrets only in Cloudflare Worker Secrets via `wrangler secret put`.
